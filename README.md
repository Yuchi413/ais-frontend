# 🛰️ SR_AICOP — AIS 船舶資訊前端介面

**SR_AICOP – AIS Vessel Information & Maritime Monitoring Frontend**

本專案是一個簡潔且可擴充的 **AIS 船舶前端顯示介面**，包含黑名單 API、船舶資料讀取、基本 UI 組件、靜態頁面等功能。
適合作為海域監控系統的前端基礎，或作為後端 AIS API / 國防監控系統的展示介面。

---

## 📌 功能特色 Features

### 🚢 1. AIS 船舶資料顯示（本地 JSON）

* 從 `vessel_data.json` 讀取船舶資訊
* 能顯示：

  * 船名
  * MMSI
  * 其他自訂欄位
* 方便整合後端 AIS API 或真實資料來源

---

### 🚨 2. 黑名單管理 API（Flask）

後端提供簡單易用的黑名單管理介面：

#### 可用功能：

* 取得黑名單清單
* 新增黑名單
* 刪除黑名單

後端結構：

```
models/blacklist_model.py
routes/blacklist_api.py
```

---

### 🌐 3. 前端 UI / 靜態頁面

`static/` 目錄包含完整的前端：

* `index.html` → 主介面
* `styles.css` → 介面風格
* `app.js` → 主邏輯
* `components/` → 模組化前端組件
* `utils.js` → 共用工具函式

此架構適合作為：

* 監控儀表板 dashboard
* AIS 資訊展示頁面
* 地圖／Cesium 前端整合的底層介面

---

## 🗂️ 專案結構 Project Structure

```
SR_AICOP/
│
├── app.py                   # Flask 主後端入口
├── requirements.txt         # Python 套件清單
│
├── models/
│   ├── blacklist_model.py
│   └── __pycache__/
│
├── routes/
│   ├── blacklist_api.py
│   └── __pycache__/
│
├── static/
│   ├── index.html
│   ├── styles.css
│   ├── app.js
│   ├── utils.js
│   ├── default.json
│   ├── vessel_data.json
│   ├── components/
│   └── processed_image.jpg (可忽略)
│
├── assets/                  # 靜態資源（如有）
├── env/                     # Python 虛擬環境（不會上 GitHub）
├── .env                     # 機密設定（不會上 GitHub）
└── .gitignore
```



---

## 🛠️ 安裝與執行（本地端）

### 1️⃣ 建立虛擬環境

```bash
python -m venv env
source env/bin/activate   # Windows: env\Scripts\activate
```

### 2️⃣ 安裝套件

```bash
pip install -r requirements.txt
```

### 3️⃣ 建立 `.env`（如需 API 金鑰）

```env
OPENAI_API_KEY=
GOOGLE_PLACES_API_KEY=
CESIUM_ACCESS_TOKEN=
```

### 4️⃣ 啟動後端

```bash
python app.py
```

### 5️⃣ 開啟前端

直接開啟：

```
static/index.html
```

或透過 Flask 提供的靜態路由。

---

## 📦 黑名單 API 端點

### 🔹 GET `/blacklist`

取得所有黑名單目標

### 🔹 POST `/blacklist`

新增可疑船舶到黑名單

### 🔹 DELETE `/blacklist/<mmsi>`

刪除黑名單船舶

---

## 🔐 安全性 Security

* `.env` 不上 GitHub（已加入 `.gitignore`）
* 虛擬環境 `env/` 不上傳
* `__pycache__` 不加入版本控制
* JSON 資料可自由替換為真實 AIS API

